{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center mt-3">
        <button class="btn btn-info">{{ button_label|default('Envoyer') }}</button>
    </div>
{{ form_end(form) }}
