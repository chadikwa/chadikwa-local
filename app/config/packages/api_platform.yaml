api_platform:
  enable_swagger_ui: false

  mapping:
    paths: ['%kernel.project_dir%/src/Entity']
  patch_formats:
    json: ['application/merge-patch+json']
  # swagger:
  #   api_keys:
  #     JWT:
  #       name: Authorization
  #       type: header